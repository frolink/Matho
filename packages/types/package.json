{
  "name": "@matho/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript domain types used across MATHO apps and packages.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
